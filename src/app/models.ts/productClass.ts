class Product {
  brand: string;
  model: string;
  size: number;
  description: string;
  price: number;

  constructor( brand, model, size, description, price ) {
    this.brand = brand;
    this.model = model;
    this.size = size;
    this.description = description;
    this.price = price;
  }
}
